<UserControl x:Class="TurkmenGuard.Views.RealTimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="Transparent">
    <StackPanel>
        <TextBlock Text="{Binding Title}" FontSize="22" FontWeight="SemiBold"
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,24"/>

        <materialDesign:Card Padding="24" Margin="0,0,0,16"
                             Background="{StaticResource CardBackgroundBrush}"
                             UniformCornerRadius="8">
            <StackPanel>
                <TextBlock Text="{Binding StatusText}" FontSize="18"
                           Foreground="{StaticResource BrandGreenBrush}" Margin="0,0,0,8"/>
                <TextBlock Text="{Binding ProcessMonitorLabel}" FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,16"/>
                <ToggleButton IsChecked="{Binding IsEnabled, Mode=TwoWay}"
                              Style="{StaticResource MaterialDesignSwitchToggleButton}"
                              Content="{Binding ToggleLabel}"/>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Padding="24"
                             Background="{StaticResource CardBackgroundBrush}"
                             UniformCornerRadius="8">
            <StackPanel>
                <TextBlock Text="{Binding EventsLabel}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ListBox ItemsSource="{Binding EventLog}" MaxHeight="280"
                         Background="Transparent" BorderThickness="0"
                         Foreground="{StaticResource TextSecondaryBrush}"
                         FontFamily="Consolas" FontSize="12"/>
            </StackPanel>
        </materialDesign:Card>
    </StackPanel>
</UserControl>
